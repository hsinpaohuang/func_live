{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "pnpm build && node .output/server/index.mjs",
    "test": "vitest --watch",
    "test:ci": "vitest",
    "deploy:vercel": "PRESET=VERCEL pnpm build"
  },
  "type": "module",
  "devDependencies": {
    "nitro-test-utils": "^0.9.0",
    "nitropack": "latest",
    "vitest": "^2.1.3"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
